# Cargo.toml
[package]
name = "gitv"
version = "0.1.0"
edition = "2024"

[dependencies]
iced = { version = "0.13", features = ["canvas"] }
git2 = "0.19"
clap = { version = "4", features = ["derive"] }

[dev-dependencies]
tempfile = "3"
